{
  "name": "no-react",
  "version": "1.0.0",
  "description": "Just tests bundling",
  "scripts": {
    "dev": "vite dev",
    "build": "npm run build:ts-default",
    "build:ts-default": "npx tsc --noEmit typescript-moduleresolution-default/src/main.ts && vite build ./typescript-moduleresolution-default",
    "test": "npm run build > /dev/null && ! (grep -i react typescript-moduleresolution-default/dist/assets/*)"
  },
  "dependencies": {
    "@fatso83/retry-dynamic-import": "file:..",
    "typescript": "^5.0.4",
    "vite": "^4.3.6"
  }
}
